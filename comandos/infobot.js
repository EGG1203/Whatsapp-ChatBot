/*module.exports = {
    name: "infobot",
    aliases: ["botinfo"],
    description: "Informações do bot",
    async execute(client, msg, args) {
        let info = client.info;
        client.sendMessage(msg.from, `*Informação de conexão*\nNome do usuário: ${info.pushname}\nMeu número: ${info.me.user}\nPlataforma: ${info.platform}\nVersão do WhatsApp: ${info.phone.wa_version}`);
    }
}*/